# Host ports / timezone
CAMREC_WEB_PORT=8080
OPENLIST_PORT=5244
TZ=Asia/Shanghai

# Local Docker build acceleration. Dockerfiles default to official upstream
# sources, so CI and environments without .env use the official registries.
# Keep these overrides in your local .env when building from mainland China.
DEBIAN_MIRROR=https://mirrors.tuna.tsinghua.edu.cn/debian
DEBIAN_SECURITY_MIRROR=https://mirrors.tuna.tsinghua.edu.cn/debian-security
PYPI_INDEX_URL=https://mirrors.tuna.tsinghua.edu.cn/pypi/web/simple
NPM_REGISTRY=https://registry.npmmirror.com

# Encryption key used for camera/SMTP/WebDAV secrets stored in SQLite.
# IMPORTANT: keep this value stable after first use.
CAMREC_SECRET_KEY=replace-with-a-long-random-secret

# Optional Hikvision HCNetSDK Linux64 runtime directory on the host.
# Point this at the directory containing libhcnetsdk.so and HCNetSDKCom/.
# The directory is mounted read-only into the internal hik-bridge container.
HIK_SDK_DIR=./hik-sdk-runtime

# OpenList container bootstrap only. Camera Recorder's WebDAV credentials,
# upload switch and retention policy are configured later in the Web UI.
OPENLIST_ADMIN_PASSWORD=replace-with-a-strong-openlist-password
OPENLIST_UID=0
OPENLIST_GID=0
